version: '3'
services:
  backend:
  container_name: 'badegewaesser'
  build:
    context: './'
    dockerfile: 'Dockerfile'
  ports:
    - '3535:3535'
  volumes:
    - '/usr/app/node_modules'
    - './:/usr/app'

